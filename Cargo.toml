[package]
name = "cursor-bridge"
version = "0.1.7"
edition = "2021"
description = "One binary. Claude Code on Cursor's backend. Zero config."
license = "MIT"
repository = "https://github.com/hkc5/cursor-bridge"
homepage = "https://github.com/hkc5/cursor-bridge"
keywords = ["cursor", "claude", "proxy", "ai", "cli"]
categories = ["command-line-utilities", "development-tools"]

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
